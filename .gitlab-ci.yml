variables:
  GIT_STRATEGY: none

# Tests for Noetic without install
noetic-catkin_build-no_install:
  image: mrm-git.e-technik.uni-ulm.de:4567/aduulm/docker_images/aduulm-ros-noetic:latest
  stage: build
  script:
    - cd $CI_PROJECT_DIR
    - build_repo_with_dependencies.sh gitlab_no_install

# Tests for Noetic with Install
noetic-catkin_build-install:
  image: mrm-git.e-technik.uni-ulm.de:4567/aduulm/docker_images/aduulm-ros-noetic:latest
  stage: build
  script:
    - cd $CI_PROJECT_DIR
    - build_repo_with_dependencies.sh gitlab_install
